A B C D -       [non percussive, melodic, ambient, decor]
E -             [placeholder for scotch]
H K S O -       [percussions, one shots, hits, drums]
G T -           [guitar, songs]

(
(
~buffers = {(
~a1 = Buffer.readChannel(s, , channels: [0]);
);
};
);
~buffers.value();
)